{
  "name": "load_assets",
  "version": "1.0.0",
  "license": "MIT",
  "description": "Generates a JavaScript file that loads all files via 'require' in a given directory. This is stored as an exported object.",
  "bin": {
    "load_required_assets": "dist/load_assets-cli.js"
  },
  "scripts": {
    "test": "mocha -r ts-node/register src/**/*.test.ts",
    "prepare": "tsc",
    "clean": "rm -rf dist/"
  },
  "author": "",
  "dependencies": {
    "indent-string": "^4.0.0",
    "lodash": "^4.17.20"
  },
  "devDependencies": {
    "@types/node": "^14.6.0",
    "@types/chai": "^4.2.12",
    "@types/mocha": "^8.0.3",
    "@types/lodash": "^4.14.160",
    "chai": "^4.2.0",
    "mocha": "^8.1.2",
    "ts-node": "^9.0.0",
    "tslint": "^6.1.3",
    "typescript": "^4.0.2"
  }
}
